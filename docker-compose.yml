services:
  web:
    build: .
    container_name: sports_school
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    env_file:
      - .env.example
    command: python manage.py runserver 0.0.0.0:8000